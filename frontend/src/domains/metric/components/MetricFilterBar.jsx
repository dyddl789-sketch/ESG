import Icon from "../../../shared/components/Icon";
import Button from "../../../shared/components/Button";
export default function MetricFilterBar({ filters, onChange }){ return <div className="filter-toolbar"><label className="search-box"><Icon name="search" size={18}/><input value={filters.keyword} onChange={e=>onChange({...filters,keyword:e.target.value})} placeholder="지표명, 코드, 사업장 검색"/></label><select value={filters.category} onChange={e=>onChange({...filters,category:e.target.value})}><option value="ALL">전체 ESG 영역</option><option value="ENVIRONMENT">환경(E)</option><option value="SOCIAL">사회(S)</option><option value="GOVERNANCE">거버넌스(G)</option></select><Button variant="outline" size="sm"><Icon name="upload" size={16}/> 엑셀 업로드</Button><Button variant="outline" size="sm"><Icon name="download" size={16}/> 엑셀 다운로드</Button></div>; }
